# Supabase 客户端配置（纯客户端 + anon key，安全依赖 RLS）
# 复制为 .env.local 填入真实值；CI 走 GitHub repo secrets。
# anon key 是公开的（会打进静态产物），必须在 Supabase 上配置 RLS。

NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key
